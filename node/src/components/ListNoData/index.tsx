import React from "react";
import styles from "./index.module.css";

const ListNoData: React.FC<ListNoData> = () => {
  return <p className={styles.nodata}>no data</p>;
};

export default ListNoData;
